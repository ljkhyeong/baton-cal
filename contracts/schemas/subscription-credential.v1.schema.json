{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cal.baton/contracts/schemas/subscription-credential.v1.schema.json",
  "title": "SubscriptionCredentialV1",
  "description": "Secret response returned once for each successful create or rotate call.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "subscriptionId",
    "token",
    "feedUrl"
  ],
  "properties": {
    "subscriptionId": {
      "type": "string",
      "format": "uuid"
    },
    "token": {
      "type": "string",
      "pattern": "^[A-Za-z0-9_-]{43}$"
    },
    "feedUrl": {
      "type": "string",
      "format": "uri",
      "pattern": "^https?://[^?#]+/calendars/v1/[A-Za-z0-9_-]{43}\\.ics$"
    }
  }
}
