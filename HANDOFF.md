# 인수인계

## 현재 상태

- 구독 해제 완료 후 목록 갱신과 자동 확인 종료 안내를 `b22d5c2`로 원본 BATON에 반영했다.
  원본 `/Users/lim/devProject/personal/manager`의 `codex/review-feature-readiness`와 검증 폴더
  `/Users/lim/devProject/personal/manager-cal-subscription`의
  `codex/cal-subscription-completion-20260905`는 같은 커밋이다. 계정 비활성화 `b19f032`를 포함한다.
  해제 완료를 확인하면 현재 검색·포함 조건으로 목록을 새로 조회한다. 90초 종료와 조회 실패에
  맞는 중단 안내를 표시하며, 상태 다시 확인을 누르면 종료 안내를 지우고 90초 확인을 다시 시작한다.
  핵심 화면 9건에 이어 최종 구독 화면 51건이 PC·모바일·WebKit에서 재시도 없이 통과했다.
  프런트 타입 검사·프로덕션 빌드도 통과했다. 화면·제품 문서 변경이며 서버·CAL 계약은 변경하지 않았다.
  이번에는 서버 테스트와 실제 캘린더 앱 검증을 다시 실행하지 않았고 원격 푸시·운영 활성화도 하지 않았다.

- BATON의 **내 계정 → 내 캘린더 구독**에 팀·시즌 이름 검색, 해제된 구독 숨기기,
  확인한 상태·시각 표시와 처리 중 상태 자동 확인을 추가했다. 기존 목록 `4a9060b`의 후속 변경이며
  원본 `/Users/lim/devProject/personal/manager`의 `codex/review-feature-readiness`에 `21d3de3`으로
  반영했다. 검증 폴더 `/Users/lim/devProject/personal/manager-cal-subscription`의
  `codex/cal-subscription-usability-20260905` 커밋 `b23755a`와 원본 커밋의 파일 내용은 같다.
  완료된 여러 시즌 기록 검색 `dccaa5d`까지 포함하며 진행 중인 계정 기능의 미커밋 변경은 보존했다.
  검색·필터는 본인 구독 전체에서 처리한 뒤 20개씩 반환한다. 조건 변경은 첫 페이지부터 조회한다.
  폐기 대기·유효 임대가 있는 기록은 숨기지 않으며 목록 자체는 CAL을 호출하지 않는다.
  열린 항목의 처리 중 상태만 3초 간격으로 최대 90초간 확인하고 완료·오류·닫기 때 중단한다.
  확인 시각은 BATON 구독 상태를 받은 시간이며 외부 캘린더 앱의 동기화 시각이 아니다.
  해제 응답 유실 뒤 조회로 완료를 확인하면 이전 오류를 정리하며 발급·해제 요청은 자동 반복하지 않는다.
  서버 전체 758개, API 문서 169개, 생성 계약 일치와 프런트 타입·프로덕션 빌드가 통과했다.
  구독·여러 시즌 검색 화면 53개 시나리오의 통과를 확인했다. WebKit은 동시 실행 중 3건,
  단독 전체 재검증 중 다른 4건이 화면 준비·이동 시간 제한에 걸렸고 해당 항목은 모두 단독 재실행에서
  통과했다. 당시 높은 시스템 부하를 관측했으며 테스트 제한 시간이나 제품 코드는 이를 위해 바꾸지 않았다.
  기존 개인 구독은 계정·시즌별 사전 ID 저장, 일회성 주소 발급·재발급·폐기와 응답 유실 복구를 제공한다.
  열람자 발급, 권한 회수·활동 중지·팀 계정 전환의 폐기 기록과 실제 CAL의 이전 URL 404는 이전
  교차 서비스 검증에서 확인했다. 이번 개선은 CAL 계약과 토큰 수명주기를 바꾸지 않았다.
  원격 푸시·운영 활성화는 하지 않았으며 기능 기본 비활성, `rc.2` 공식 게시·채택과 실제 앱 검증은 남아 있다.
- 남은 기능 검토 항목의 CAL 작업을 진행했다. 내부 복구 실행 GET과 시즌 진단 GET을 추가하고
  실제 응답 스키마·진행·완료·미수신·인증을 검증했다. 조회는 읽기 전용이며 기존 완료 시각을 유지한다.
- `cal.b4ton.com`용 [운영 구성](docs/operations.md)에 Nginx HTTPS·요청 제한, Prometheus,
  Alertmanager와 준비 상태 점검을 추가했다. 격리된 로컬 TLS에서 200·304·429, 내부 경로 차단,
  실제 중단·재시작에 따른 알림 발생·해제와 로그·메트릭 토큰 비노출을 검증했다. CI도 같은 스모크를 실행한다.
- [앱별 구독 안내와 호환성 확인표](docs/calendar-subscription-guide.md)를 작성했다. 사용자가
  `cal.b4ton.com` 사용을 제안했으며 현재 실제 서버·공인 HTTPS·프록시·Prometheus 환경은 없다.
  실제 앱 테스트와 운영 DNS·알림 채널 연결은 미실행이다. 서버와 DNS 업체 정보가 필요하다.
- 실제 HTTP 최초 적재 1,000개는 38.694초, 같은 시즌 두 작업자의 변경 1,000개는 65.741초였다.
  혼합 조건부 조회 p95는 5.359ms였고 UID·개정·취소·재구축 ETag 일치를 확인했다.
  [성능 기준](docs/performance-baseline.md)에 범위와 재현 명령을 기록했다. 운영 규모와 목표 시간은
  아직 합의되지 않아 묶음 수신 API는 추가하지 않았다.

- 작업 후보 `1.1.0-rc.2`에 ID 지정 구독 생성 PUT을 추가했다. BATON이 미리 저장한 구독 ID로 최초
  생성하고, 재전달은 기존 토큰·상태·세대를 유지한 중복 또는 시즌 충돌로 처리한다. 상태 GET과
  명시적 rotate로 응답 유실을 복구한다. 기존 POST는 유지하며 DB 마이그레이션은 없다.
  BATON 개발 브랜치의 소비 구현과 로컬 교차 검증은 완료했으며 `rc.2` 공식 게시·BATON 고정은 남아 있다.
- 시즌 단위 MVP와 계약·영속성·HTTP·iCalendar·OCI 스모크 테스트가 구현되어 있다.
- BATON 생산자 연동은 수동 회차 `ALL_DAY`, 자동 회차 `ZONED_LOCAL_POINT`, 루틴 마감
  `UTC_POINT`와 변경·취소·중복·역순 전달까지 검증했다.
- 현재 생산자 기준은 [불변 안정 릴리스 `contracts-v1.0.0`](https://github.com/ljkhyeong/baton-cal/releases/tag/contracts-v1.0.0)이다.
  태그 커밋은 `fd081a742b7c09a7ace53bb445ce1380c533c19e`, 자산
  `baton-cal-contracts-1.0.0.zip`의 SHA-256은
  `b1aea8fed42c7b3f38320e1e0d883bd99c4d78e09d5b1dbddd4c90b2154146a7`이며 BATON 고정을 완료했다.
  이 자산에는 루트 `LICENSE`가 없어 계약 의미를 유지한 `1.0.1` 호환 보완판의 별도 태그·자산과
  BATON 재고정 절차를 준비했다. 아직 게시하지 않았다.
- 불변 사전 릴리스 `1.1.0-rc.1`은 기존 128 KiB 문서 상한에 JSON 구조 자원 제한을 추가하고 `prod`
  데이터베이스가 로컬 기본값을 상속하지 않게 한다. HTTP 표준에 맞춘 Last-Modified 상한과
  ETag 우선 조건부 요청, 36자 표준 UUID 입력, RFC 6750 형식의 내부 Bearer 설정, 취소 뒤
  재활성화, 예상 밖 `500` 비밀 비노출과 데이터베이스 제한 시간의 `503 SERVICE_BUSY` 회귀 검증,
  iCal4j 4.3.0 단일 시간대 규칙 권위도 포함한다. 같은 시즌의 서로 다른 항목을 동시에 수신하는
  시즌 잠금 회귀 테스트와 로컬 기본 테스트, 실행 JAR, 계약 ZIP, 별도 투영 부하 테스트를 통과했다.
  태그 커밋 `f1573edef1adf900570cd55f9bd7d7044566b6bd`, 자산 SHA-256
  `7ac97568c8b10e4ac2dadb9d463312c1a5985c424a3a3a9e69c2bd8ee6dd376f`로 게시했고, BATON이 일정·시즌
  이름·복구 요청 스키마와 자산을 고정해 실제 CAL 컨테이너 교차 서비스 검증을 완료했다. 운영 안정
  기준은 정식 버전 승격 전까지 `1.0.0`이다.
- 공개 구독은 `ACTIVE` 상태·토큰 해시·구독 세대가 모두 일치할 때만 조회된다. V4~V6 배포의 구버전
  종료·롤백 제한은 유지한다. V7는 별도 시즌 이름 테이블을 추가하며, 모든 구버전을 종료한 뒤
  이름 전달을 시작한다. 이름을 수신한 뒤 pre-V7 공존·롤백은 금지한다.
- 복구 모드는 외부 설정 `BATON_CAL_RECOVERY_MODE`로 켜며 기본값은 `false`다. 활성화하면
  구독 생성·회전만 `503 RECOVERY_IN_PROGRESS`로 차단한다. V8은 시즌별 일정·시즌 이름 상태를
  다이제스트로 대조하는 매니페스트와 전체 시즌 집합의 완료 신호를 저장한다. 완료 전 재검증과
  완료 요청 재시도는 멱등하며 완료 뒤 내용 변경은 거부한다. `COMPLETED` 응답을 확인한 운영자가
  같은 구독 세대에서 모드를 해제해 모든 인스턴스를 재시작하며 CAL은 설정을 자동 해제하지 않는다.
- 내부 상태 조회는 `GET /internal/api/v1/calendar-items/{sourceItemId}`와
  `GET /internal/api/v1/subscriptions/{subscriptionId}`로 제공한다. 채택한 개정 번호·상태·원본 수정
  시각과 구독 상태·현재 세대 일치 여부만 반환하며, 토큰·해시·피드 URL·세대 UUID는 제외한다.
  복구 모드에서도 조회할 수 있다. 취소·폐기·세대 불일치, 중복·역순 수신, 인증·UUID·없는 자원과
  실제 응답 JSON Schema를 기존 HTTP 테스트에서 검증한다.
- 시즌 표시 이름은 `PUT /internal/api/v1/seasons/{seasonId}/calendar-metadata`의
  `{revision, displayName}`으로 수신한다. 시즌 전용 개정 번호로 중복·역순·충돌을 처리하고 같은
  시즌 잠금 안에서 저장과 피드 재구축을 커밋한다. 이름이 없으면 기존 기본 이름을 유지하고,
  이름 변경은 일정 UID·SEQUENCE나 구독 자격 증명을 바꾸지 않는다.
- BATON은 최초 시즌·이름 수정·다음 시즌 생성과 같은 트랜잭션에 V29 이름 아웃박스를 기록한다.
  기존 일정 테이블은 바꾸지 않으며 전달 작업자·임대·재시도 코드를 공유한다.
  실제 BATON 유스케이스·MySQL·운영 HTTP 클라이언트와 CAL 후보 컨테이너를 연결해 생성과 이름
  수정이 같은 구독 피드에 반영되는 것을 검증했다. 스케줄러를 기다리지 않고 전달 서비스를 호출했다.
  실제 PostgreSQL 백업을 복원하고 BATON 최신 이름을 같은 개정 번호로 재전달하는 흐름도 검증했다.
  새 구독 세대에서 이전 URL의 `404`, 복구 모드의 발급 `503`과 수동 해제 뒤 최신 이름을 확인했다.
  기존 시즌·캡처 중단 기간은 `BACKFILL`로 보정하고, `REPLAY`는 최신 이름을 같은 개정 번호로
  재전달 대기에 넣는다. 실패 행은 자동 재처리하지 않으며 전체 일정 복구 완료를 판정하지 않는다.
  `BATON_CAL_SEASON_METADATA_ENABLED=false`, 보정 모드 `OFF`와 안정 핀 `1.0.0`을 유지한다.
  후보 계약 채택과 CAL V7 배포 확인, 실제 캘린더 앱 검증 전에는 운영에서 켜지 않는다.
- OCI 스모크는 Java 25 비루트 이미지, Flyway V1~V8, 준비 상태, SIGTERM 종료, 동일 세대 재시작,
  PostgreSQL 논리 백업·복원과 복원 세대 펜스, 복구 모드의 발급 차단과 수동 해제 뒤 회전을 검증한다.
  시즌 이름도 백업 시점으로 복원한 뒤 최신 개정으로 재전달한다.
  고정된 취소·이름 다이제스트로 누락 상태의 검증 거부와 `VERIFIED`·`COMPLETED`, 완료 재시도의
  최초 시각 보존을 확인한 뒤 복구 모드를 해제하도록 보완했다.
- 일정 수신·내부 인증·투영 재구축·시즌 잠금은 식별자와 비밀값이 없는 Micrometer 지표를 남긴다.
  Prometheus 형식은 `/actuator/prometheus`로 제공하며 `prod`에서는 기본 `8081` 관리 포트로
  분리한다. 공개 프록시는 관리 포트를 노출하지 않아야 한다.
  PostgreSQL 잠금 대기는 기본 5초, SQL 실행과 Spring 트랜잭션은 기본 30초이며, 제한 시간 초과는
  `503 SERVICE_BUSY`와 `Retry-After: 1`로 반환한다.
- CI는 풀 리퀘스트에서 읽기 권한으로 OCI 이미지를 검증만 하고, `main` 푸시 작업에만 패키지 쓰기
  권한을 부여해 스모크를 통과한 같은 이미지를
  `ghcr.io/ljkhyeong/baton-cal:{전체 Git 커밋 SHA}`로 게시한다. 외부 액션은 전체 커밋 SHA로
  고정했고 Gradle 의존성은 잠금 파일과 SHA-256 검증 메타데이터를 함께 검사한다. Gradle,
  GitHub Actions와 Docker Compose 갱신은 Dependabot이 매주 제안하도록 구성되어 있다. 과거 계약
  태그에서 갈라진 `release/contracts-` 풀 리퀘스트는 일반 검증과 별도로 정확한 브랜치 HEAD에서
  계약 ZIP을 만들고 검증한다.
- 시즌 전체 투영 수동 부하 측정은 PostgreSQL 18.6에서 500~10,000개를 각 5회 실행했다. 10,000개는
  719~733밀리초, 2,459,060바이트였으며 [성능 기준](docs/performance-baseline.md)에 기록했다.
- 시간대 지정 현지 시각은 iCal4j 4.3.0 내장 Olson `2025a`의 원문 TZID만 허용한다. DST 공백도
  같은 `VTIMEZONE`에서 만든 규칙으로 판정해 Java 런타임 TZDB와 규칙 권위를 섞지 않는다.
- 구현 계약과 운영 절차는 [PRD-0002](docs/PRD/0002_mvp-contract/spec.md), 기술 선택은
  [ADR-0002](docs/ADR/0002_technology-stack/adr.md), 계약 게시 이력과 명령은
  [계약 릴리스 현황](docs/contract-release-history.md)과
  [계약 릴리스 절차](docs/contract-release-procedure.md)를 기준으로 한다.

## 검증 명령

2026-09-05에 기본 테스트 111개가 실패·오류·건너뜀 없이 통과했다. 별도 HTTP 부하 테스트,
실행 JAR·계약 ZIP과 `baton-cal:remaining-features-smoke` 이미지 빌드를 완료했다. 같은 이미지에서
실제 PostgreSQL 백업·복원, 고정 매니페스트 완료·재시도, 모드 해제·회전, SIGTERM 종료와
HTTPS 프록시·요청 제한·알림 발생·해제·토큰 비노출을 확인했다. 새 rc.2의 BATON 구독·진단 소비 코드는
위의 로컬 개발 브랜치에서 검증했으며 공식 릴리스 채택과는 구분한다.

```shell
./gradlew --no-daemon test bootJar verifyContractsZip
./gradlew --no-daemon projectionLoadTest
./gradlew --no-daemon ingestionLoadTest -PloadItemCount=1000
./gradlew --no-daemon bootBuildImage --imageName=baton-cal:smoke
./scripts/smoke-oci-image.sh baton-cal:smoke
./scripts/smoke-operations.sh baton-cal:smoke
```

`projectionLoadTest`와 `ingestionLoadTest`는 기본 테스트에서 제외한 수동 성능 측정이다.

안정 계약 자산의 다운로드와 검증 명령은 [계약 릴리스 현황](docs/contract-release-history.md)을 따른다.

## 다음 작업

1. `1.1.0-rc.2`를 검토·게시하고 공식 자산·증명으로 BATON에 재고정한다. 팀 권한·구독 통합과 로컬
   구독·진단 소비 검증은 완료했다. 정식 채택 뒤 `1.1.0` 안정 버전을 준비한다.
   `1.0.x` 계열을 별도로 유지해야 하면 루트 `LICENSE`를 포함한 `1.0.1` 호환 보완판을 게시한다.
2. 내부 Bearer를 실제 비밀 관리 시스템에 연결하고 현재 값·이전 값 회전을 운영 환경에서 훈련한다.
3. 실제 서버와 DNS 업체를 정한 뒤 준비한 운영 구성을 배포하고 `cal.b4ton.com`의 공인 HTTPS·
   인증서 갱신·운영 알림 수신 채널·외부 점검을 연결한다. 실제 프록시·추적에서도 토큰 비노출을 확인한다.
4. 기본 요청 제한과 알림 임계값을 실제 제공자 공유 IP·수신량에 맞춰 조정하고 로그 보존 정책을 정한다.
5. 운영 RTO/RPO, 백업 저장소·암호화와 실제 환경 복원 훈련을 확정하고 완료 응답 뒤 복구 모드를
   해제하는 운영 자동화를 연결한다.
6. 배포 서버에서 GHCR digest 고정과 builder 갱신 절차를 적용한다. 단일 호스트 배포 절차는 운영 문서에 준비했다.
7. 후속 iCal4j가 Olson `2025a`보다 최신 데이터를 포함하면 의존성 잠금, 골든 바이트와 ETag를
   검토한 새 계약 후보로 갱신한다.
8. 실제 캘린더 앱의 이름 표시·갱신을 검증하고 후보 계약을 채택한 환경에서 이름 보정·전달을
   활성화한다. 아웃박스 재전달 준비, CAL 수신 완료와 전체 복구 완료는 구분한다.
9. 운영 데이터 규모·동시 수신량·복구 목표 시간을 정하고 HTTP 부하 측정을 반복한다. 목표를 넘으면
   크기를 제한한 묶음 수신·묶음당 재구축을 새 계약으로 검토한다.

## 현재 제한

- `1.1.0-rc.1` 게시와 BATON 생산자 검증 고정은 완료했지만 안정 버전 승격, 실제 운영 활성화와
  공개 배포는 하지 않았다.
- CI 계약 팩은 보존 기간 90일을 요청하는 변경 검토용 임시 산출물이며 안정 의존성이 아니다.
- GHCR 게시 자동화는 구성했지만 실제 운영 배포, 운영 비밀 수명주기, Prometheus 백엔드 연결과
  실제 프록시 관측 경로 검증은 남아 있다. 저장소의 로컬 운영 구성과 알림 전달은 검증했다.
- 저장소 복원 훈련은 대표 픽스처만 사용한다. 전체 매니페스트 계약은 구현했지만 실제 운영 데이터와
  백업 저장소를 사용한 훈련, 완료 확인 뒤 복구 모드 해제는 운영자 또는 상위 오케스트레이션이 담당한다.
- 일정·구독 상태와 시즌 진단 조회는 개별 자원 진단용이다. 복구 실행 조회는 저장된 과거 완료 기록을
  반환한다. 조회가 현재 원본 완전성을 새로 판정하거나 복구 모드를 해제하지 않으며,
  토큰 원문을 복구하지 않는다. ID 지정 PUT 생성의 응답 유실은 사전에 알고 있는 ID로 조회할 수 있지만,
  기존 POST 생성의 응답 유실과 rotate 응답 유실 시 같은 토큰을 다시 받는 제한은 유지한다.
- 시즌 표시 이름은 CAL 수신·투영, BATON 원본 변경의 아웃박스 전달과 기존 이름 보정·재전달 준비까지
  구현했다. 후보 계약의 정식 채택과 실제 캘린더 앱의 이름 갱신 검증은 아직 하지 않았다.
- iCal4j 4.3.0 내장 시간대 데이터는 Olson `2025a`다. CAL은 이를 입력 검증과 출력의 단일 권위로
  사용하므로 더 최신 IANA 시간대 식별자와 규칙은 다음 의존성 갱신 전까지 지원하지 않는다.
- 현재 구현은 MVP이며 운영 준비 완료를 뜻하지 않는다.

## 저장소

- 공개 원격 저장소: <https://github.com/ljkhyeong/baton-cal>
