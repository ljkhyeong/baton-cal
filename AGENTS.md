# BATON CAL 작업 지침

## 작업 진행

- `HANDOFF.md`에서 현재 상태를 확인하고 `README.md`와 관련 PRD·ADR의 필요한 부분을 읽는다.
- 사용자의 명시적 요청을 스킬의 일반 절차보다 우선한다. 승인된 작업은 구현·검증·커밋까지 진행하며,
  결과를 바꿀 미결정 사항만 질문한다. 권한이나 지시 때문에 멈추면 해당 근거를 구체적으로 알린다.
- 응답, PR 리뷰, 문서와 주석은 짧고 명확한 실무 용어로 한글 작성한다. API 필드·코드 식별자·경로·
  표준명은 원문을 유지한다.
- 제품 동작은 `docs/PRD/`, 구조 결정은 `docs/ADR/`에 갱신한다. `HANDOFF.md`에는 남은 작업과 제한을
  기록하고, 구현·검증·배포하지 않은 내용을 완료로 쓰지 않는다.

## 서비스 계약

- BATON이 일정·반복·시간대·마감과 최종 권한을 결정한다. CAL은 확정 스냅샷을 읽기 전용 iCalendar로
  변환한다. RELAY는 푸시, GO는 앱 링크, ROUND는 방 참여권을 담당하며 서비스 간 DB·세션·자격 증명을 공유하지 않는다.
- 이벤트는 원본 커밋 이후 중복·역순으로 도착할 수 있다. 같은 재전달은 상태를 바꾸지 않고 낮은 개정 번호는
  무시한다. `UID`를 유지하고 원본 개정 번호가 증가할 때만 `SEQUENCE`를 높이며 삭제는 취소 표식으로 남긴다.
- IANA 시간대와 UTC·현지 시각·종일 날짜의 의미를 보존한다. 시간 의존 코드는 `Clock`을 주입하고,
  같은 투영은 같은 캘린더 바이트·`ETag`·`Last-Modified`를 유지한다.
- 구독 토큰은 발급 응답에서만 노출하고 해시만 저장한다. 회전·폐기는 이전 토큰을 즉시 무효화한다.
  토큰 원문·작업공간 키·세션·권한 증서를 Git·DB·로그·추적·메트릭·캘린더 내용에 넣지 않는다.

## 구현과 검증

- 같은 책임의 검증은 한 곳에 둔다. HTTP 입력, 도메인 규칙, DB 무결성처럼 실패 책임이 다른 검증은 유지한다.
- Kotlin 표준 라이브러리와 JDK·Spring·고정한 라이브러리 API를 우선한다. 계약을 충족하지 못하는 부분만
  최소 코드로 보완하고 그 차이를 회귀 테스트로 남긴다.
- [개발 검증 절차](docs/development.md)에 따라 이전 결과와 변경 범위를 확인하고 필요한 검증만 실행한다.
  실패 원인을 확인한 뒤 필요한 범위만 재실행하고, 입력·환경이 같은 성공 결과는 재사용한다. 필수 CI는 완료한다.
- 긴 문서는 `rg`로 위치를 찾고 필요한 절만 읽는다. 인수인계에는 최신 검증 근거와 남은 작업만 남긴다.
- 계약·투영·구독·복구 작업의 세부 주의점은 `baton-cal-flows` 스킬에서 확인한다.

## 커밋

- 작업을 마치면 본인 변경을 목적별로 나눠 `종류: 한글 요약`으로 커밋한다.
  종류: `기능`, `수정`, `문서`, `테스트`, `설정`, `빌드`, `배포`, `운영`, `리팩터`.
- 작업 브랜치는 `codex/` 접두사를 사용한다. 푸시·공식 릴리스·운영 배포는 요청 범위에 포함될 때 진행한다.
