# 개발 검증 절차

## 실행 전

- 현재 저장소·브랜치·미커밋 변경을 확인한다. 다른 작업 트리는 `git worktree list`로 확인하고,
  과거 작업에 적힌 경로를 현재 프로젝트 경로로 가정하지 않는다.
- [인수인계](../HANDOFF.md)의 최근 검증을 확인하고 이후 변경 파일을 `git diff <검증 커밋>`으로 비교한다.
  미추적 파일·의존성·설정·테스트 환경 변경도 포함한다. 커밋명이나 브랜치명만으로 재사용을 결정하지 않는다.
- 변경한 동작, 필요한 검증, 완료 조건을 정한다. 결과를 재사용하면 근거를 적고, 재실행은 입력 변경·실패·
  환경 차이·필수 CI 중 이유를 밝힌다. 입력을 확인할 수 없으면 해당 범위를 다시 검증한다.

## 검증 선택

| 변경 | 로컬 검증 |
| --- | --- |
| 문서·지시 | `git diff --check`, 바뀐 내용과 참조 경로 확인 |
| 스킬 | `./scripts/validate-skill.sh <스킬 디렉터리>` |
| 단일 기능 | `./gradlew --no-daemon test --tests '<관련 테스트 클래스>'` |
| HTTP·계약 | 관련 HTTP 응답의 JSON Schema 테스트, `./gradlew --no-daemon verifyContractsZip` |
| 공통 코드·여러 계층 | `./gradlew --no-daemon check` |
| DB 스키마 | 관련 저장소·마이그레이션 통합 테스트 |
| 이미지·운영 구성 | [README의 이미지 검증](../README.md#oci-이미지-검증), [운영 검증](operations.md#재현-가능한-로컬-검증) |
| 구독 세대·복원 | 실제 백업 복원, 과거 URL `404`, 최신 재전달·완료 후 새 발급 |
| BATON 연동·복구 다이제스트 | 요청 범위에서 BATON 실제 직렬화기와 CAL의 HTTP 교차 검증 |
| 성능 측정 | 목표·환경을 정한 뒤 [성능 기준](performance-baseline.md)의 부하 테스트 |

DB 통합 테스트에는 Docker가 필요하다. `check`는 일반 테스트·계층 규칙·검사 스크립트·계약 ZIP을 검증하며 부하 측정·이미지
빌드·운영 스모크는 포함하지 않는다. PR·공식 릴리스의 필수 CI는 로컬 검증과 별도로 완료한다.

## 편집 직후와 종료 전 검사

```bash
# 편집 전 한 번. 중간에 커밋해도 이 기준을 유지한다.
python3 -B scripts/agent_feedback.py start
# 파일을 작성한 도구 호출 직후
python3 -B scripts/agent_feedback.py files
# 종료 직전. 출력된 review.diff 전체를 읽고 검토한다.
python3 -B scripts/agent_feedback.py final
```

| 시점 | 검사 범위 |
| --- | --- |
| 파일 작성 직후 | Git 공백 오류, 변경 JSON·Python·셸 구문. Kotlin·Java·Gradle 입력 변경 시 증분 컴파일 |
| 종료 직전 | 시작 커밋부터의 diff, 스테이징·미추적 파일, 운영 코드 전체의 ArchUnit 규칙, 검사 스크립트 회귀 테스트 |
| CI | `check`에 위 구조 검사와 기존 테스트·계약 검증 포함 |

파일 검사는 같은 내용이면 생략한다. 테스트 소스 변경은 `testClasses`, 그 밖의 JVM 입력 변경은
`classes`를 사용하며, Gradle이 바뀐 입력만 다시 컴파일한다. JSON 구문 검사는 JSON Schema 검증을
대신하지 않고, YAML·SQL·Markdown의 내용 규칙도 위 표의 관련 검증으로 확인한다.

종료 검사는 Docker 없이 실행된다. 출력은 `build/agent-feedback/<세션 해시>/`의 로그와 `review.diff`에
보관한다. 전체 diff의 책임 분리·중복·누락은 에이전트가 직접 읽고 검토한다. 자동 규칙의 범위는
[ADR-0003](ADR/0003_agent-feedback/adr.md)에 정의한다.

### Codex 자동 실행

[`.codex/hooks.json`](../.codex/hooks.json)이 사용자 요청 시 기준을 기록하고, `apply_patch`·편집 도구·
셸 도구 완료 후 파일 검사, `Stop` 시 종료 검사를 실행한다. 도구가 여러 파일을 쓰면 한 번에 검사한다.
이미 진행 중인 요청에는 기준을 다시 잡지 않는다. 종료 검사가 처음 통과하면 전체 diff 검토를 요청하며
한 번 더 진행한다. 이후 같은 변경으로 종료하면 재실행하지 않고, 파일이 바뀌면 다시 검사한다.

설정은 Codex CLI `0.144.5`의 [공식 훅 계약](https://learn.chatgpt.com/docs/hooks)을 기준으로 한다.
프로젝트의 `.codex` 설정과 각 훅은 신뢰 검토가 필요하다. 저장소에서 Codex CLI를 열고 `/hooks`로
세 훅을 확인·신뢰한 뒤 사용한다. 새 정의는 신뢰 전까지 건너뛰며, 실행 중인 앱 세션에 즉시 적용됐다고
가정하지 않는다. 훅이 실행되지 않는 환경에서는 위 명령을 직접 실행한다.

검사 실패는 오류와 로그 위치를 에이전트에 돌려준다. 권한·Java·의존성 문제라면 환경을 먼저 고치고
실패한 범위만 다시 실행한다. 같은 변경의 실패는 자동 재실행하지 않으며 종료 시 실패로 남긴다.
환경을 고쳤다면 `final`을 수동 실행한다. 명령 하나의 제한은 180초이며, 초기 의존성 설치나 환경 준비는 훅 밖에서
진행한다. 자동 훅은 관련 동작 테스트와 필수 CI를 대신하지 않는다.

수동 작업의 세션은 `CODEX_THREAD_ID`가 있으면 그 값, 없으면 `manual`이다. 여러 터미널에서 작업하면
각 명령에 같은 `--session <작업명>`을 붙인다. 이미 편집했다면 `start --base <편집 전 커밋>`으로 기준을
지정한다. 상태가 삭제됐거나 환경·규칙이 바뀌었다면 `final`을 수동 실행해 검증한다.

## 재실행과 종료

- Gradle 캐시와 증분 검사를 사용한다. `clean`·`--rerun-tasks`는 기본 명령에 붙이지 않는다.
  새 성능 측정이나 캐시 문제 조사처럼 실제 재실행이 필요할 때 이유를 남긴다.
- `UP-TO-DATE`·`FROM-CACHE`는 기존 성공 결과 재사용으로 보고한다. 새 테스트 실행으로 쓰지 않는다.
  관련 입력·환경이 같은 단순 브랜치 이동이나 fast-forward 병합은 전체 재검증 이유가 아니다.
- 실패하면 제품 오류, 테스트 오류, 환경·도구 오류를 먼저 구분한다. 환경을 고치지 않은 채 같은 명령을
  반복하거나 원인 확인 전에 전체 테스트로 넓히지 않는다.
- DB·브라우저·이미지처럼 자원을 많이 쓰는 검증은 같은 장비의 다른 실행을 확인해 겹치지 않게 조정한다.
  시간 초과는 로그와 부하를 확인한 뒤 실패한 범위부터 재실행한다. 다른 작업의 프로세스를 임의 종료하지 않는다.
- 실패 로그와 결과 파일을 확인한 뒤 재실행한다. Gradle은 `build/test-results/test/`와
  `build/reports/tests/test/`를 사용한다. 필요한 범위가 통과하면 종료하고 변경·실패 근거 없이 확대하지 않는다.

인수인계에는 최신 관련 검증만 `기준 커밋·미커밋 여부 / 명령·범위 / 환경 / 실행·캐시 재사용 /
결과·제한`으로 기록한다. 과거 결과는 커밋과 검토 문서에 남기고 매 작업마다 완료 내역을 누적하지 않는다.

## 스킬 검증 환경

`validate-skill.sh`는 공통 지침의 `~/.codex/venvs/skill-validation/bin/python`을 사용한다.
검증기 위치는 `${CODEX_HOME:-$HOME/.codex}/skills/.system/skill-creator/scripts/quick_validate.py`다.

공용 환경이나 PyYAML이 없으면 다음 명령으로 준비한다. 이후에는 같은 환경을 재사용하며,
프로젝트별 가상환경을 만들거나 시스템 Python에 패키지를 설치하지 않는다.

```bash
python3 -m venv ~/.codex/venvs/skill-validation
~/.codex/venvs/skill-validation/bin/python -m pip install 'PyYAML==6.0.3'
```

## 작업 기록에서 확인한 개선 근거

CAL의 `MVP 우선 개발 여부 검토`(작업 ID `019ff107-1dfa-70a0-a3c1-6378c2d8d760`)와
`검토: 추가 개선 기능`(`01a06eec-b670-7af1-9bd3-f7fac157217e`)의 관련 기록을 확인했다.

| 확인한 사례 | 적용한 개선 |
| --- | --- |
| 9월 2일 12:32 UTC, 9월 5일 11:45 UTC에 각각 `yaml` 누락 오류를 연속 재현 | 스킬 검증 환경 재사용 |
| 긴 파일·검색 결과가 잘려 같은 내용을 다시 읽음 | `rg -n`으로 위치를 찾고 해당 절만 읽으며, 결과 요약과 원본 로그 경로를 분리 |
| `git show 6c8eec6:HANDOFF.md`의 구독 개선 기록에서 WebKit 시간 초과 뒤 단독 재실행 통과, 당시 높은 장비 부하 관측 | 무거운 검증 동시 실행을 줄이고 환경 확인 후 실패 범위부터 재실행. 부하를 확정 원인으로 단정하지 않음 |
| 이전 BATON 경로를 현재 프로젝트로 오인했고 인수인계는 217줄로 누적 | 현재 작업 트리 확인, 최신 검증과 남은 작업만 인계 |

필요한 충돌 해결 후 전체 테스트나 새 성능 측정은 낭비로 분류하지 않았다. 앱의 작업 조회에서 본문이
비어 있는 최근 기록은 동일 요청을 반복하지 않고 해당 CAL 작업의 로컬 기록으로 보완했다.
