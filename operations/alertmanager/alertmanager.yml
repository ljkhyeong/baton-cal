route:
  receiver: operations
  group_by: [alertname]
  group_wait: 5s
  group_interval: 10s
  repeat_interval: 4h
receivers:
  - name: operations
    webhook_configs:
      - url_file: /run/secrets/alert-webhook-url
        send_resolved: true
